# This is a sample environment file for deployment.
# When deploying to Vercel/Netlify/etc, copy this variable to your deployment settings.

# Replace the URL below with your actual deployed Render backend URL.
# Note: Keep the /api at the end!
VITE_API_URL=https://your-backend-service-name.onrender.com/api
